# Copy to .env.local and fill in. .env.local is git-ignored.

# Your deployed Netlify site URL. Used by Netlify Identity in the browser.
# Locally this can be left blank.
VITE_NETLIFY_SITE_URL=

# Stripe keys go here when you add cart + checkout. The storefront runs without
# them — products are static placeholders until then.
# STRIPE_SECRET_KEY=
# VITE_STRIPE_PUBLISHABLE_KEY=
